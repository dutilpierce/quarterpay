name: quarterpay
description: QuarterPay – One quarterly payment. Three months of breathing room.
publish_to: "none"

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  # State / routing
  get: ^4.6.6

  # Core
  http: ^1.2.2
  shared_preferences: ^2.3.2
  google_fonts: ^6.2.1
  flutter_dotenv: ^5.1.0

  # Firebase core (optional but already scaffolded)
  firebase_core: ^3.6.0

  # OpenAI (matching new API I provided)
  dart_openai: ^6.1.1

  # Plaid (new 5.x API)
  plaid_flutter: 5.0.5

  # Icons
  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/images/beach.png
    - .env
